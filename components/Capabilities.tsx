// This component has been deprecated and merged into Solution.tsx to match the sequential vertical card layout required by the audit.
// Keeping an empty component to prevent build errors if referenced elsewhere, but it should be removed from App.tsx.

import React from 'react';

export const Capabilities: React.FC = () => {
  return null;
};